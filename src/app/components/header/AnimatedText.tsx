import React from 'react'

const AnimatedText = ({ text }: { text: string }) => {
    return (
        <>
            <div>
                { text }
            </div>
        </>
    )
}

export default AnimatedText;
